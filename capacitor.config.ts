import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PdfViewerTearsOnPinchZoom_ionic_angular_iOS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
